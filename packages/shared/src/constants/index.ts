export * from './ai-limits';

export const APP_NAME = 'one-down';

/** Workspace package name — echoed by the tRPC health query to prove the shared import end-to-end. */
export const SHARED_PACKAGE_NAME = '@one-down/shared';
