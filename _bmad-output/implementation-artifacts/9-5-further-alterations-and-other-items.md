- Could we set up some sort of method for seeding tasks? Using the data synchronization via accounts setup in order to set the app up with the exact content that we need to be able to test what we're trying to test, for the end to end test suites, avoid duplication etc?
- The ripple animation does not follow the same proper maths as in the design. There was a negative gap which cut the cycle short, which was set to a default value, which appears to have been missed off the implemented animation entirely.
- When the shadow expands outwards from the card, it leaves a hard inner edge. Ideally, I don't want that hard inner edge. I don't know whether it's possible to have just the outline of a shape that's the same size as the card and then blur that outline using a blur filter that also animates? Perhaps worth exploring. Alternatively, we could just use a shape with a blurred shadow and the shape and the shadow expands and fades so we don't get that hard inner edge line. 
- When the AI learns something from a task that is more general, it should go into a general notes section which you can access from the settings screen and edit and it should just have bullet point information about the user or about how they like their tasks generally. Also in the AI prompt it should make it clear that there doesn't have to be a learning, that there should only be a learning if there's something that is outside the scope of what the AI currently knows or would have done automatically.
- For the button to show you the smallest step. At the moment the cards jump up when it shows. Instead they should p animate a gentle ease in out push up and then reverse that animation when moving to the next card that doesn't have that keeps coming around button.
- The triage button should show (if there are cards to triage) next to the click Add button always, not just when the context change window is expanded.
- The notification dots on the contexts should stay if there are cards there that need attention even if those contexts are currently selected. At the moment the notification button disappears if the context is currently selected, which I think is misleading.
- When a card is triaged, the AI should learn about what kind of tasks are quick wins and what kind of tasks are big time modes. from what the user picks or corrects and this should go into the global context info with the learning (if relevant).
- The date picker should be themed blue to match triage mode, if accessed from within the triage mode, when setting a deadline.
- When a blueprint card is save and next clicked, the card should fly straight upwards and away, revealing the next card below.
- The five stars should be awarded using a toast that says triage cleared rather than one down, but it should be otherwise the same.
- The bonuses that are applied to cards should be in the window of two to four days before the deadline, if there's less than two days before the deadline they don't apply. It counts as zero days before the deadline if the deadline is today.
- The edit card icon incorrectly takes you to the doing page on bonus cards
- The bottom of the "what should be different" box on the doing page should align to the top of the keyboard when it is focused, but actually the notes box aligns to the keyboard instead so the task list is Unnecessarily squished.
- I'm thinking perhaps we have an importance attribute as well? Basically something that encapsulates how bad it would be if this thing didn't get done by the deadline. we should probably come up with a pithy single word for that. Criticality, perhaps? Then we can have chill, important and critical as the options. 
- We need to have some sort of prioritization system. So something that is critical and three days out should probably be prioritized over something that is chill but is one day out.. I think we will have to have a hidden urgency metric. This metric should be calculated 
